import 'package:flutter/material.dart';

class GridViewUniversities extends StatelessWidget {
  const GridViewUniversities({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("GridView"),
    );
  }
}